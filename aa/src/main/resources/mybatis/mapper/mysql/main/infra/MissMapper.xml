<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aa.module.miss.MissDao">

	<resultMap
		type="com.aa.module.miss.MissDto" id="resultMapObj"></resultMap>
	<select id="mbList" resultMap="resultMapObj">
	
	
	
	
	
	</select>
	<select id="uaList" resultMap="resultMapObj">
	select 
	a.uaSeq,
	a.uaName
	from
	uAnimal as a
	where 1=1 and
	a.uaDelNy = 0
	and
	a.user_uSeq = #{user_uSeq}
</select>

	<select id="uaOne" resultMap="resultMapObj">
	select 
	a.uaName,
	a.uaBOD,
	a.uaBreed,
	a.uaGenderCd,
	(select b.cName from code as b where b.cSeq = a.uaGenderCd) as cName
	from uAnimal as a
	where 1=1 and
	
	a.uaSeq = #{uAnimal_uaSeq}
	</select>
	<insert id="mbInsert">
		insert into missingBoard
		(
		mbDetailAddr,
		uAnimal_uaSeq,
		mbFindCd,
		mbPostalCode,
		mbStreetAddr,
		mbEtc,
		mbReward,
		mbLat,
		mbLon,
		mbRegDate,
		mbTitle,
		mbDate
		)values
		(
		#{mbDetailAddr},
		#{uAnimal_uaSeq},
		7,
		#{mbPostalCode},
		#{mbStreetAddr},
		#{mbEtc},
		#{mbReward},
		#{mbLat},
		#{mbLon},
		now(),
		#{mbTitle},
		#{mbDate}
		)
	
	</insert>
</mapper>